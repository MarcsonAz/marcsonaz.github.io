name: Atualizar Projetos JSON

on:
  push:
    branches:
      - main
    # Ignora mudanças no próprio arquivo projetos.json para evitar um loop infinito de commits
    paths-ignore:
      - 'projetos.json'

jobs:
  update-json:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Permissão necessária para o bot conseguir fazer o commit

    steps:
      - name: Fazer o checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Executar script de extração
        run: node fetch-repos.js

      - name: Fazer commit e push do novo JSON
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add projetos.json
          # O comando abaixo verifica se houve alteração no projetos.json. 
          # Se houver, ele faz o commit e o push de volta para a main.
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: atualiza projetos.json com repositórios recentes" && git push)